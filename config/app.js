const config = {
    db: { 
        mysql: {
            host:'localhost',
            user:'root',
            password:'',
            database:'selling-projects'
        }
    },
    // listPerPage: env.LIST_PER_PAGE || 10,
  };
  
  
  module.exports = config;