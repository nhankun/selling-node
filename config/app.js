const config = {
    db: { 
        mysql: {
            host:'localhost',
            user:'root',
            password:'vanthinh133',
            database:'selling_projects',
            port: 3306,
        }
    },
    // listPerPage: env.LIST_PER_PAGE || 10,
  };
  
  
  module.exports = config;